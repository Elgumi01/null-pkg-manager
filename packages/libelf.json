{
  "name": "libelf",
  "description": "ELF object file access library, part of the Elfutils package, providing routines to read and manipulate ELF binaries and required by Mesa's radeonsi Gallium driver.",
  "version": "0.193",
  "dependencies": [],
  "download": [
    "curl -fL --retry 3 --retry-delay 2 -o elfutils-0.193.tar.bz2 https://sourceware.org/elfutils/ftp/0.193/elfutils-0.193.tar.bz2",
    "tar -xf elfutils-0.193.tar.bz2"
  ],
  "build": [
    "cd elfutils-0.193 && ./configure --prefix=\"$PREFIX\" --disable-debuginfod --enable-libdebuginfod=dummy",
    "cd elfutils-0.193 && make $MAKEOPTS"
  ],
  "install": [
    "cd elfutils-0.193 && make -C libelf DESTDIR=\"$DESTDIR\" install",
    "install -Dvm644 elfutils-0.193/config/libelf.pc \"$DESTDIR$PREFIX/lib/pkgconfig/libelf.pc\"",
    "rm -f \"$DESTDIR$PREFIX/lib/libelf.a\""
  ]
}
