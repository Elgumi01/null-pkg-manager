# npkg build environment
# This file is sourced (as KEY=VALUE pairs) into npkg's environment
# before building packages. DO NOT set DESTDIR here — it's overridden
# automatically per-package during install.

MAKEOPTS="-j12"
CFLAGS="-O2 -pipe"
CXXFLAGS="-O2 -pipe"
PREFIX=/usr
